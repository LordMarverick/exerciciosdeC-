//codigo com a função de calculo e conversão// 

console.writeline("Digite a altura do retânglo?");
float altura = convert.toint 32 (console.readline());

console.writeline ("Digite a base do retângulo?");
float base = converto.toint 32 (console.readline());


double total = altura + base;
float = total =0;

double votos altura = (altura/retânglo)*100;
double votos nulos = (base/retânglo)*100;

contel.writeline ("total o retângulo:"+total);
console.Readkey();